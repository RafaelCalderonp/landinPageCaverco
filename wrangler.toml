name = "caverco-partners-landing"
compatibility_date = "2024-09-01"
pages_build_output_dir = "."

# El binding de D1 (base de datos "caverco_contacts") se agrega desde el
# dashboard de Cloudflare Pages: Settings -> Functions -> D1 database
# bindings, una vez creada la base de datos (Workers & Pages -> D1 ->
# Create database). No se declara aquí para no romper el deploy con un
# id de ejemplo. Ver README.md para el paso a paso.
